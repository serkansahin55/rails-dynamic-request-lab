<h1> Students Index Page </h1>

<div>
  <% @students.each do |student| %>
    <div><%= student.first_name + " " + student.last_name %></div>
    <p> <%= link_to(student.first_name, "students/#{student.id}") %></p>
  <% end %>
</div>